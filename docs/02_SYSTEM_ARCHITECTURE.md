# JobChat System Architecture

**Version:** 3.0
**Last Updated:** 2026-01-13
**Architecture:** 3-Stage Sequential Filter with Maps API

---

## 1. 시스템 개요

### 1.1 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              CLIENT LAYER                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                     React + TypeScript + Vite                        │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐     │    │
│  │  │ useChat  │  │useGeo    │  │Components│  │   API Client     │     │    │
│  │  │  Hook    │  │location  │  │(7 files) │  │   (Axios)        │     │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘     │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                     │                                        │
│                              HTTP/REST (JSON)                                │
└─────────────────────────────────────┼───────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              API LAYER                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                    FastAPI + Uvicorn                                 │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐     │    │
│  │  │ /chat    │  │ /health  │  │ /stats   │  │ /geocode/reverse │     │    │
│  │  │ POST     │  │ GET      │  │ GET      │  │ GET              │     │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘     │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                     │                                        │
└─────────────────────────────────────┼───────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            SERVICE LAYER                                     │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────────┐       │
│  │   GeminiService  │  │   JobSearch      │  │    MapsService       │       │
│  │                  │  │   Service        │  │                      │       │
│  │  - AI Chat       │  │  - Salary Filter │  │  - Distance Matrix   │       │
│  │  - Job Filter    │  │  - Format Jobs   │  │  - Reverse Geocode   │       │
│  │  - Parse Query   │  │  - Get Stats     │  │  - Travel Time       │       │
│  └──────────────────┘  └──────────────────┘  └──────────────────────────┘   │
│           │                    │                         │                   │
└───────────┼────────────────────┼─────────────────────────┼──────────────────┘
            │                    │                         │
            ▼                    ▼                         ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          EXTERNAL SERVICES                                   │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────────┐       │
│  │   Google Gemini  │  │    Firestore     │  │   Google Maps API    │       │
│  │   2.0 Flash-Lite │  │                  │  │   Distance Matrix    │       │
│  └──────────────────┘  └──────────────────┘  └──────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      ▲
                                      │
┌─────────────────────────────────────┼───────────────────────────────────────┐
│                            DATA LAYER                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                    JobKorea Crawler                                  │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐     │    │
│  │  │ Scraper  │  │Normalizer│  │ MVP      │  │ Firestore        │     │    │
│  │  │          │  │(Salary/  │  │ Filter   │  │ Writer           │     │    │
│  │  │          │  │ JobType) │  │          │  │                  │     │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘     │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 기술 스택

### 2.1 Frontend

| 기술 | 버전 | 용도 |
|------|------|------|
| React | 19.x | UI 프레임워크 |
| TypeScript | 5.x | 타입 안전성 |
| Vite | 5.4.x | 빌드 도구 |
| Tailwind CSS | 3.x | 스타일링 |
| Axios | 1.x | HTTP 클라이언트 |

### 2.2 Backend

| 기술 | 버전 | 용도 |
|------|------|------|
| Python | 3.9+ (권장 3.11+) | 런타임 |
| FastAPI | 0.109.x | Web 프레임워크 |
| Uvicorn | 0.27.x | ASGI 서버 |
| Pydantic | 2.6.x | 데이터 검증 |
| httpx | 0.27.x | 비동기 HTTP |
| google-generativeai | 0.8.x | Gemini API |
| googlemaps | 4.10.x | Maps API |
| google-cloud-firestore | 2.16.x | 데이터베이스 |

### 2.3 Crawler

| 기술 | 용도 |
|------|------|
| httpx | 비동기 HTTP |
| BeautifulSoup4 | HTML 파싱 |
| asyncio | 병렬 처리 |

### 2.4 Infrastructure

| 서비스 | 용도 |
|--------|------|
| Firebase Hosting | Frontend 배포 |
| Cloud Run | Backend 배포 |
| Cloud Run Jobs | Crawler 스케줄링 |
| Firestore | NoSQL 데이터베이스 |

---

## 3. 컴포넌트 상세

### 3.1 Frontend Components

```
frontend/src/
├── App.tsx                 # 루트 컴포넌트
├── main.tsx               # 엔트리포인트
├── components/
│   ├── ChatWindow.tsx     # 메인 채팅 컨테이너
│   ├── MessageList.tsx    # 메시지 목록
│   ├── MessageBubble.tsx  # 개별 메시지
│   ├── JobCardList.tsx    # 공고 목록
│   ├── JobCard.tsx        # 개별 공고 카드
│   ├── InputBox.tsx       # 입력 영역
│   └── LoadingIndicator.tsx
├── hooks/
│   ├── useChat.ts         # 채팅 상태 관리
│   └── useGeolocation.ts  # GPS 위치 관리
├── services/
│   └── api.ts             # API 클라이언트
└── types/
    └── index.ts           # TypeScript 타입
```

#### Component Hierarchy

```
App
└── ChatWindow
    ├── LocationIndicator (📍 현재 위치)
    ├── ErrorBanner
    ├── MessageList
    │   └── MessageBubble (n개)
    │       └── JobCardList
    │           └── JobCard (n개)
    ├── LoadingIndicator
    └── InputBox
```

### 3.2 Backend Structure

```
backend/app/
├── main.py                # FastAPI 앱 설정
├── config.py              # 환경 설정 (Pydantic Settings)
├── routers/
│   └── chat.py            # /chat 엔드포인트
├── services/
│   ├── gemini.py          # AI 서비스 (770+ lines)
│   ├── job_search.py      # 검색/필터링 서비스
│   ├── maps.py            # Maps API 서비스 (270+ lines)
│   └── location.py        # 위치 참조 데이터
├── models/
│   └── schemas.py         # Pydantic 모델
└── db/
    └── firestore.py       # Firestore 클라이언트
```

### 3.3 Crawler Structure

```
crawler/app/
├── main.py                # 크롤러 엔트리
├── config.py              # 크롤러 설정
├── scrapers/
│   └── jobkorea.py        # 잡코리아 스크래퍼 (600+ lines)
├── normalizers/
│   ├── job_type.py        # 직무 정규화
│   ├── salary.py          # 연봉 파싱
│   └── location.py        # 위치 정규화
└── db/
    └── firestore.py       # Firestore 저장
```

---

## 4. API 설계

### 4.1 Endpoints

| Method | Path | 설명 |
|--------|------|------|
| POST | /chat | 자연어 검색 |
| GET | /health | 헬스 체크 |
| GET | /stats | 서비스 통계 |
| GET | /geocode/reverse | 역지오코딩 |
| GET | / | 서비스 정보 |

### 4.2 POST /chat

**Request:**
```json
{
  "message": "강남역 30분 거리 웹디자이너, 연봉 4천 이상",
  "conversation_id": "uuid-string",
  "page": 1,
  "page_size": 20,
  "user_lat": 37.497942,
  "user_lng": 127.027621
}
```

**Response:**
```json
{
  "success": true,
  "response": "강남역 30분 내 웹디자이너 공고 15건을 찾았습니다!",
  "jobs": [
    {
      "id": "jk_12345678",
      "title": "웹디자이너",
      "company_name": "(주)테크컴퍼니",
      "location": "서울 강남구",
      "salary": "4,000~5,000만원",
      "experience": "경력 2년 이상",
      "employment_type": "정규직",
      "deadline": "상시",
      "url": "https://...",
      "travel_time_minutes": 15,
      "travel_time_text": "지하철 15분"
    }
  ],
  "pagination": {
    "page": 1,
    "page_size": 20,
    "total_count": 15,
    "total_pages": 1,
    "has_next": false,
    "has_prev": false
  },
  "search_params": {
    "job_type": "웹디자이너",
    "salary_min": 4000,
    "location_query": "강남역",
    "max_commute_minutes": 30
  },
  "conversation_id": "uuid-string"
}
```

### 4.3 GET /health

**Response:**
```json
{
  "status": "healthy",
  "version": "3.0.0",
  "environment": "development",
  "services": {
    "firestore": "connected",
    "gemini": "available",
    "maps_api": "available"
  }
}
```

---

## 5. 데이터베이스 설계

### 5.1 Firestore Collection: `jobs`

**Document ID:** `jk_{jobkorea_id}` (예: `jk_12345678`)

**Schema:**
```typescript
interface JobDocument {
  // 기본 정보
  id: string;                    // 고유 ID
  title: string;                 // 공고 제목
  company_name: string;          // 회사명
  url: string;                   // 원문 URL
  source: string;                // 출처 (jobkorea)

  // 위치 정보
  location: string;              // 시/구 수준 ("서울 강남구")
  location_full?: string;        // 상세 주소 (Maps API용)

  // 연봉 정보
  salary_text?: string;          // 원문 ("3,500~4,500만원")
  salary_min?: number;           // 최소 연봉 (만원)
  salary_max?: number;           // 최대 연봉 (만원)

  // 경력/고용 정보
  experience_type?: string;      // "신입", "경력", "신입/경력"
  experience_min?: number;       // 최소 경력 (년)
  experience_max?: number;       // 최대 경력 (년)
  employment_type?: string;      // "정규직", "계약직" 등

  // 기타
  deadline?: string;             // 마감일
  job_type_raw?: string;         // 원본 직무 분류
  mvp_category?: string;         // MVP 카테고리

  // 상태/메타
  is_active: boolean;            // 활성 상태
  created_at: Timestamp;         // 생성일
  updated_at: Timestamp;         // 수정일
  crawled_at: Timestamp;         // 크롤링 일시
}
```

### 5.2 인덱스

```javascript
// firestore.indexes.json
{
  "indexes": [
    {
      "collectionGroup": "jobs",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "is_active", "order": "ASCENDING" },
        { "fieldPath": "mvp_category", "order": "ASCENDING" }
      ]
    },
    {
      "collectionGroup": "jobs",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "is_active", "order": "ASCENDING" },
        { "fieldPath": "updated_at", "order": "DESCENDING" }
      ]
    }
  ]
}
```

---

## 6. 3-Stage Filter Pipeline

### 6.1 Architecture Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                    Phase 0: 필수 정보 수집                        │
│                                                                  │
│  사용자 입력 → Gemini AI → Function Call 또는 추가 질문          │
│                                                                  │
│  필수: job_type, salary_min                                      │
│  선택: location_query, max_commute_minutes, use_current_location │
└──────────────────────────────────────────────────────────────────┘
                              │
                    search_jobs() 함수 호출
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                    Stage 1: 직무 필터 (AI)                        │
│                                                                  │
│  Input:  전체 활성 공고 (~1,000건)                               │
│  Process: AI가 job_type과 의미적으로 매칭되는 공고 선별           │
│  Output: 직무 매칭 공고 (~100건)                                 │
│                                                                  │
│  복수 직무 지원: "개발자 or 디자이너" → 각각 필터 후 합집합       │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                    Stage 2: 연봉 필터 (DB)                        │
│                                                                  │
│  Input:  Stage 1 결과 (~100건)                                   │
│  Logic:  salary_min >= 요청값 OR salary_min IS NULL              │
│          (NULL = 회사내규, 협상가능)                             │
│  Output: 연봉 조건 충족 (~80건)                                  │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                    Stage 3: 거리 필터 (Maps API)                  │
│                                                                  │
│  조건:   location_query 또는 user_coordinates 존재 시            │
│  Input:  Stage 2 결과 (~80건, 주소 있는 것만)                    │
│  Process:                                                        │
│    1. 출발지 파싱 (텍스트 → 좌표 or GPS 좌표 직접 사용)          │
│    2. Google Maps Distance Matrix API 호출 (배치 25개씩)         │
│    3. max_commute_minutes 이내 필터링                            │
│  Output: 최종 결과 (~20건), 이동시간순 정렬                       │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                    Result: 페이지네이션 + 응답                    │
│                                                                  │
│  - 20건/페이지                                                   │
│  - 각 공고에 travel_time_minutes, travel_time_text 추가         │
│  - AI 응답 텍스트 생성                                           │
└──────────────────────────────────────────────────────────────────┘
```

### 6.2 Stage 처리 위치

| Stage | 처리 위치 | 파일 |
|-------|----------|------|
| Phase 0 | Gemini AI | `services/gemini.py` |
| Stage 1 | Gemini AI | `services/gemini.py` |
| Stage 2 | Python | `services/job_search.py` |
| Stage 3 | Google Maps API | `services/maps.py` |

---

## 7. 서비스 클래스 상세

### 7.1 GeminiService (`services/gemini.py`)

```python
class GeminiService:
    """V3.2: Single Function Call 패턴"""

    def __init__(self):
        self.chat_model = ...      # Function Call용
        self.filter_model = ...    # 직무 필터용
        self._conversations = {}   # 대화 히스토리

    async def process_message(
        self,
        message: str,
        conversation_id: str,
        page: int,
        page_size: int,
        user_coordinates: Optional[tuple]
    ) -> Dict[str, Any]:
        """메인 엔트리: 메시지 처리 및 검색 실행"""

    async def _execute_search_pipeline(
        self,
        job_type: str,
        salary_min: int,
        location_query: str,
        max_minutes: int,
        user_coordinates: Optional[tuple],
        use_current_location: bool
    ) -> Dict[str, Any]:
        """3-Stage 파이프라인 실행"""

    async def _parse_job_types(self, query: str) -> List[str]:
        """복수 직무 파싱"""

    async def _parse_location_query(self, query: str) -> List[str]:
        """위치 쿼리 파싱"""

    async def _filter_by_job_type(
        self,
        job_type: str,
        jobs: List[Dict]
    ) -> List[Dict]:
        """Stage 1: AI 직무 필터"""

    async def _filter_by_distance(
        self,
        jobs: List[Dict],
        origins: List[str],
        max_minutes: int,
        is_coordinates: bool
    ) -> List[Dict]:
        """Stage 3: 거리 필터"""
```

### 7.2 MapsService (`services/maps.py`)

```python
class MapsService:
    """Google Maps API 연동 서비스"""

    def __init__(self):
        self.client = googlemaps.Client(key=...)

    def is_available(self) -> bool:
        """API 키 설정 확인"""

    async def calculate_travel_times(
        self,
        origin: str,
        destinations: List[str],
        mode: str = "transit"
    ) -> List[Dict]:
        """배치 이동시간 계산 (25개씩)"""

    async def reverse_geocode(
        self,
        lat: float,
        lng: float
    ) -> Optional[str]:
        """좌표 → 주소 변환"""

    async def filter_jobs_by_travel_time(
        self,
        jobs: List[Dict],
        origin: str,
        max_minutes: int
    ) -> List[Dict]:
        """이동시간 기준 필터링"""
```

### 7.3 JobSearch Functions (`services/job_search.py`)

```python
async def get_all_active_jobs() -> List[Dict]:
    """전체 활성 공고 조회"""

def filter_by_salary(jobs: List[Dict], salary_min: int) -> List[Dict]:
    """Stage 2: 연봉 필터"""

def format_job_results(jobs: List[Dict]) -> List[Dict]:
    """API 응답용 포맷팅"""

async def get_job_stats() -> Dict[str, Any]:
    """통계 조회"""
```

---

## 8. 배포 아키텍처

### 8.1 Production Environment

```
                         ┌─────────────────┐
                         │   DNS/CDN       │
                         │   (Cloudflare)  │
                         └────────┬────────┘
                                  │
              ┌───────────────────┼───────────────────┐
              │                   │                   │
              ▼                   ▼                   ▼
    ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
    │ Firebase        │ │ Cloud Run       │ │ Cloud Run Jobs  │
    │ Hosting         │ │ (Backend)       │ │ (Crawler)       │
    │                 │ │                 │ │                 │
    │ - Static Files  │ │ - Auto-scaling  │ │ - Scheduled     │
    │ - SPA Routing   │ │ - Container     │ │ - Daily Run     │
    └─────────────────┘ └─────────────────┘ └─────────────────┘
              │                   │                   │
              └───────────────────┼───────────────────┘
                                  │
                                  ▼
                         ┌─────────────────┐
                         │   Firestore     │
                         │   (NoSQL DB)    │
                         └─────────────────┘
```

### 8.2 배포 명령어

**Backend:**
```bash
cd backend
gcloud run deploy jobchat-api --source . --region=asia-northeast3
```

**Frontend:**
```bash
cd frontend
npm run build
firebase deploy --only hosting
```

**Crawler:**
```bash
cd crawler
gcloud run jobs create jobchat-crawler \
  --image gcr.io/$PROJECT_ID/jobchat-crawler \
  --region=asia-northeast3
```

---

## 9. 보안 설계

### 9.1 인증/인가

| 영역 | 현재 상태 | 보호 방법 |
|------|----------|----------|
| API 접근 | 공개 | CORS로 오리진 제한 |
| 데이터베이스 | 서버 전용 | Service Account |
| API Keys | 서버 사이드 | 환경변수 (.env) |

### 9.2 CORS 설정

```python
# backend/app/config.py
ALLOWED_ORIGINS = [
    "http://localhost:5173",
    "http://localhost:5174",
    "http://localhost:5175",
    # ... 5176-5180
    "http://localhost:3000",
    "https://jobchat.web.app",  # Production
]
```

### 9.3 입력 검증

```python
# backend/app/models/schemas.py
class ChatRequest(BaseModel):
    message: str = Field(..., min_length=1, max_length=1000)
    page: int = Field(default=1, ge=1)
    page_size: int = Field(default=20, ge=1, le=100)
    user_lat: Optional[float] = Field(None, ge=-90, le=90)
    user_lng: Optional[float] = Field(None, ge=-180, le=180)
```

---

## 10. 에러 처리

### 10.1 에러 응답 형식

```json
{
  "success": false,
  "response": "죄송합니다. 처리 중 오류가 발생했습니다.",
  "jobs": [],
  "pagination": {
    "page": 1,
    "page_size": 20,
    "total_count": 0,
    "total_pages": 0,
    "has_next": false,
    "has_prev": false
  },
  "error": "Error message detail"
}
```

### 10.2 에러 코드

| HTTP | 상황 | 처리 |
|------|------|------|
| 400 | 잘못된 요청 | 입력 검증 실패 |
| 500 | 서버 오류 | 예외 발생 시 |
| 503 | 서비스 불가 | 외부 API 장애 |

---

## 11. 모니터링

### 11.1 Health Check

**Endpoint:** `GET /health`

**체크 항목:**
- Firestore 연결 상태
- Gemini API 가용성
- Maps API 가용성

### 11.2 통계

**Endpoint:** `GET /stats`

**제공 정보:**
- 전체 공고 수
- 활성 공고 수
- 마지막 크롤링 시간

### 11.3 로깅

```python
# 검색 로그
logger.info(f"Pipeline 시작: job_type={job_type}, salary_min={salary_min}")
logger.info(f"Stage 1 결과: {len(filtered)}건")
logger.info(f"Stage 2 결과: {len(stage2_jobs)}건")
logger.info(f"Stage 3 결과: {len(final_jobs)}건")
```

---

## 12. 확장 포인트

### 12.1 크롤러 확장

```python
# 새 크롤러 추가
crawler/app/scrapers/
├── jobkorea.py      # 기존
├── saramin.py       # 추가 예정
└── wanted.py        # 추가 예정
```

### 12.2 필터 확장

```python
# Stage 추가 예정
# Stage 4: 경력 필터
# Stage 5: 회사 규모 필터
```

### 12.3 AI 모델 교체

```python
# config.py
GEMINI_MODEL = "gemini-2.0-flash-lite"  # 현재
# GEMINI_MODEL = "gemini-2.0-pro"       # 업그레이드 가능
```
